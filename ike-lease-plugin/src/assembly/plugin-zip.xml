<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0
          http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <!--
        An IntelliJ plugin distribution is a zip containing one directory
        named for the plugin, with its jars under lib/:

            ike-lease-plugin/
              lib/
                ike-lease-plugin-<version>.jar
                ike-lease-core-<version>.jar

        The dependency set carries this jar plus the runtime dependencies
        — which is exactly the core; the provided IDE platform jars stay
        out. The lease.sh wrapper globs the installed lib/ for the core
        jar, so installing the zip is also what serves the headless CLIs.

        Install through Settings > Plugins > Install Plugin from Disk.
    -->
    <id>plugin</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>true</includeBaseDirectory>
    <baseDirectory>${project.artifactId}</baseDirectory>
    <dependencySets>
        <dependencySet>
            <outputDirectory>lib</outputDirectory>
            <useProjectArtifact>true</useProjectArtifact>
            <scope>runtime</scope>
        </dependencySet>
    </dependencySets>
</assembly>
