<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>network.ike</groupId>
        <artifactId>ike-lease</artifactId>
        <version>4-SNAPSHOT</version>
    </parent>

    <artifactId>ike-lease-core</artifactId>
    <packaging>jar</packaging>

    <name>IKE Working-Set Leases — Core</name>
    <description>
        The lease protocol (the exact port of lease.sh v2, golden-tested
        against a frozen reference) and the git-state materializer, as a
        plain-Java JPMS library with no IDE dependency — one core, thin
        hosts (IKE-Network/ike-issues#1057, #1067). The IntelliJ plugin
        embeds it; lease.sh execs its CLIs; the ws: goals may depend on it
        directly, which is what retires the goal-to-$HOME-script coupling
        (IKE-Network/ike-issues#1005).
    </description>

    <dependencies>
        <!-- The core's tests run real git in temp directories, and the
             protocol suite runs the frozen lease.sh v2 reference beside
             the port — no mocks; the thing under test is behavior at the
             process boundary. -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.11.4</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- The module descriptor makes surefire run tests on the
                 module path, where JPMS resource encapsulation hides the
                 frozen lease.sh v2 reference from the golden suite. The
                 tests spawn processes and read files — nothing modular —
                 so they run on the classpath; the descriptor still
                 compiles and ships in the artifact. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <useModulePath>false</useModulePath>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
